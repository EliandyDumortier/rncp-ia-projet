# ==============================================================================
# requirements.txt — Étape 4 (Application React)
# Projet : Système de recommandation de K-Dramas par IA
#
# L'application de l'étape 4 est désormais une application React (TypeScript)
# servie par nginx. Les dépendances sont gérées par npm (voir package.json).
# Ce fichier liste les outils Python encore utilisés pour le linting et les
# tests de l'API IA (intégration).
# ==============================================================================

# --- Tests de l'API IA (intégration étape 3 → étape 4) ---
pytest==8.2.2
pytest-cov==5.0.0
requests==2.32.3

# --- Linting & Qualité de code (outils complémentaires) ---
ruff==0.5.0
black==24.8.0